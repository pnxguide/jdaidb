class Tuple:
    def __init__(self, types: list[type], values: list[object]):
        self.types = types
        self.values = values
    
    # TODO(A1): implement this
    def to_bytes(self) -> bytes:
        return bytes()
