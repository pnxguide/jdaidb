from jdaidb.sunbears.dataframe import DataFrame
from jdaidb.structure.hash_table import HashTable

def op_distinct(frame: DataFrame) -> DataFrame:
    # TODO(A3): Verify whether the frame has only one column and it is integer

    # TODO(A3): Create a new DataFrame based on columns in the result
    new_dataframe = DataFrame()
    # Add more code...

    # TODO(A3): Create a hash table and add entry to the new_dataframe if not collide

    # Return new_dataframe
    return new_dataframe
